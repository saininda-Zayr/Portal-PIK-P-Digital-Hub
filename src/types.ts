export interface NavItem {
  title: string;
  href: string;
  icon: string;
}

export interface StatCard {
  title: string;
  value: string | number;
  change: string;
  icon: string;
}

export interface BerAKHLAKValue {
  key: string;
  title: string;
  description: string;
  featureLink: string;
}
